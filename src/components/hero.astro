<section class="relative bg-white overflow-hidden">
  <div class="relative z-10 max-w-7xl mx-auto px-4 py-20 grid md:grid-cols-2 gap-8 items-center">
    
    <!-- Texto -->
    <div class="space-y-6 text-center md:text-left flex flex-col items-center md:items-start">
      <h1 class="mb-[0.5rem] text-4xl font-semibold text-gray-900">
        Cuidamos con amor cada <strong class="text-cyan-500 font-bold">PATITA</strong>
      </h1>
      <p class="mb-[0.5rem] text-gray-600">
        Cuidado natural, profesional y cercano siempre
      </p>
      <button class="border border-cyan-700 bg-white text-cyan-700 px-6 py-2 rounded-md hover:border-white hover:bg-cyan-50 transition">
        Reserva Ahora
      </button>
    </div>

    <!-- Imagen con círculo detrás -->
    <div class="relative flex justify-center items-center">
      <!-- Círculo detrás de la imagen -->
      <div class="absolute w-64 h-64 bg-blue-100 rounded-full -z-10 sm:w-72 sm:h-72"></div>

      <!-- Imagen principal -->
      <img src="/img/hero/hero_img.webp" alt="Veterinaria con perro" class="w-64 sm:w-80 object-contain relative z-10" />
    </div>

  </div>
</section>
